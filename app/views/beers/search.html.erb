<div class="page-header">
	<h1>Search<% if params[:term] %><% unless params[:term].empty? %>ing for "<em><%= params[:term] %></em>"
		 <small style="font-size: 40%"> get a <a href="/beers/search/<%= params[:term] %>">permalink</a> to this search</small>
		 <%end%></h1>
		<%end%>
</div>


<div style="max-width: 600px; margin: 0 auto">
	<%= form_tag beers_search_path, :method => 'get' do %>
	<div class="form-group">
		<div class="input-group">
			<%= text_field_tag :term, params[:term], :class => "form-control input-lg", :placeholder => "Beer name..."%>
				<div class="input-group-addon">
					<%= submit_tag "Search", :name => nil, :class => "btn btn-success", :style => "height: 32px" %>
				</div>
		</div>
	</div>
	<%end%>
</div>

<% if params[:term] %>
<% unless params[:term].empty? %>

<%= render 'searchlist' %>

<%end%>
<%end%>